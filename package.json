{
  "name": "vibra",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "bin": {
    "shazam": "dist/cli.js"
  },
  "scripts": {
    "start": "node dist/cli.js",
    "build": "tsc && cp src/vibra.wasm dist/vibra.wasm",
    "prestart": "npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.11.19",
    "typescript": "^5.3.3"
  }
}
